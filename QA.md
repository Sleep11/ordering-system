# 深度检测与 QA 记录

> 版本：v2.5.1.6
> 更新：2026-07-31  
> 范围：功能、性能、桌面/移动端布局、文档、部署配置

## 检测方法

- 静态检查：`node --check` 覆盖 `app.js`、`api.node.js`、`auth.node.js`、`kv-adapter.node.js`。
- 运行时检查：本地 HTTP 服务 + Playwright mock API，覆盖管理员登录、订单、菜品保存、自取、恢复页、桌面和移动端布局。
- 线上检查：`bawei.rth1.xyz` HTML、CDN 静态资源、登录/菜单 API。

## 功能验证

| 项目 | 结果 | 说明 |
|---|---|---|
| 日期默认今天 | 通过 | 正常时段默认今天；20:30 后按餐别规则自动改为明天午餐 |
| 餐别自动选择 | 通过 | 08:00、11:30、20:30 分钟边界均正确 |
| 日期范围 ±30 天 | 通过 | `min/max` 正确写入日期输入框 |
| 菜品下拉与价格回显 | 通过 | 48 项测试菜单正常渲染，价格随选择显示 |
| 保存菜品 | 通过 | 价格失焦自动保存；再点保存不会重复提交 |
| 菜品 note 保留 | 通过 | 后端保存菜单时按 ID 合并旧 `note` |
| 自取减免按钮 | 通过 | 管理员可见；320px 下自动换行且不被裁剪 |
| 订单刷新 | 通过 | 8 秒自动刷新，哈希相同跳过渲染 |
| 管理员用户刷新 | 通过 | 30 秒限频，不再每 8 秒请求 `get-users` |
| 恢复页 | 通过 | 自动读取主页面 token，不再直接 403 |
| 版本号 | 通过 | 当前为 `v2.5.1.6`，仅管理员显示 |
| 订单金额字段 | 通过 | `应收/减免/实收/退款` 显示正确 |
| 自取减免重算 | 通过 | 切换后应收减 1、已付款订单退款变为 1 |
| 数据库自动迁移 | 通过 | 首次请求自动补齐旧订单金额字段 |
| 保存菜品格式 | 通过 | 兼容表单提交的 JSON 字符串 |
| Toast 时长 | 通过 | 普通提示 4.5 秒，错误提示 6 秒 |
| 退款按钮 | 通过 | 退款后状态变为“已退款”，退款金额变为绿色 |
| 提示码 | 通过 | Toast 显示 `MSG-XXXX` 并提供复制按钮 |
| 即时刷新 | 通过 | 增删改后立即刷新页面数据 |
| 菜单格式修复 | 通过 | 兼容 Retinbox 多种请求体结构，不再出现 `MSG-VVG2` |
| 多餐食订餐 | 通过 | 同一人可添加多行餐食，数量独立加减 |
| 订餐列表布局 | 通过 | 下拉无价格、价格框加大、数量控件分行 |
| 今日统计单行 | 通过 | 桌面/移动端均单行横向滚动 |
| 菜品管理滚动 | 通过 | 菜品列表内部滚动，不影响页面整体 |
| 周月报金额 | 通过 | 使用应收/实收，减免与退款正确反映 |
| 多餐食编辑 | 通过 | 编辑多餐食订单时保留原有明细 |
| 订单列表价格 | 通过 | 恢复原价展示并压缩行高 |
| 菜品管理同步 | 通过 | 增删菜品后订餐下拉框立即同步 |
| 保存菜品兜底 | 通过 | 增加 `menuJson` 兼容请求，修复 `MSG-VVG2` |
| 下个对话规则 | 通过 | 触发后自动更新文档、推送 main、部署、给出起始问题 |
| 版本更新规则 | 通过 | 每次提交更新版本，大更新可跳号 |
| 下个对话触发 | 通过 | 自动更新文档、推送 main、部署、同步 bugfix |

## 性能优化

### 改动
- 管理员 `get-users` 请求限频 30 秒。
- 菜品保存增加签名去重，避免失焦和保存按钮连续触发两次 API。
- 订单/报表 KV 读取改为每批 10 条并发。
- 订单金额字段在部署后自动迁移，无需手动改库。
- 清理 CSS 重复规则和无效 `.col-note` 样式。
- 移动端表单控件字号 16px，减少 iOS 聚焦缩放。

### 实测结果
- 登录后 9 秒内：`get-orders` 出现 2 次（初始 + 自动刷新），`get-users` 仍为 1 次。
- 菜品改价失焦：`update-menu` 1 次；随后点保存：仍为 1 次。
- 桌面 1440×900：页面无横向溢出。
- 移动端 390×844、320×568：页面均无横向溢出。

## 布局验证

| 视口 | 批量表格 | 顶部栏 | 今日统计/自取 | 结论 |
|---|---|---|---|---|
| 1440×900 | 正常 | 无重叠 | 同行 | 通过 |
| 390×844 | 内部横向滚动 | 无重叠 | 同行 | 通过 |
| 320×568 | 内部横向滚动 | 无重叠 | 自动换行、满宽 | 通过 |

## 已知问题

- Retinbox 平台会覆盖 API 的 `Cache-Control`，线上实测为 `private, max-age=1`，不是代码中的 `no-store`。
- Retinbox 会重写静态资源为 CDN 哈希 URL，原始 `?v=` 参数不会保留在最终 HTML 中。
- 默认菜品当前为 47 款，需求为 48 款；缺第 48 款菜名、价格和权重，需业务确认。
- 记住密码功能目前将明文密码保存在 `localStorage`，建议后续改为服务端可撤销刷新令牌。
- 仓库 `origin` 仍嵌有 GitHub token，建议尽快移除并轮换。

## 分支建议

当前工作分支为 `bugfix`，功能上可继续使用，但命名过于宽泛。建议后续统一为：

```text
bugfix/performance-mobile-ui
```

或按 Codex 默认规范：

```text
codex/performance-mobile-ui
```

本地 `h0tfix` 分支拼写错误且远端已删除，建议删除；`develop`、`feature`、`release` 目前都指向同一提交，后续按实际用途再拆分。

## 回归命令

```bash
node --check app.js
node --check api.node.js
node --check auth.node.js
node --check kv-adapter.node.js
```

完整 Playwright 脚本在开发机 `.reasonix/` 下按需执行，不提交 Git。

## v2.5.1.6 新增检测 (2026-07-31)

| 项目 | 结果 | 说明 |
|---|---|---|
| AGENTS.md 规则 | 通过 | Deep Test Rule 已添加，触发词"测试" |
| 版本号一致性 | 通过 | app.js、index.html、README、QA 均为 v2.5.1.6 |

## v2.5.1.5 新增检测 (2026-07-31)

| 项目 | 结果 | 说明 |
|---|---|---|
| 页面布局 | 通过 | 侧边栏 176px，主内容区无最大宽度限制，桌面端全宽利用 |
| 订单列表价格 | 通过 | `.money-price` 类着色为蓝色，五列金额可区分 |
| 订单列表行高 | 通过 | Grid 布局行高压缩至 34px，交替行背景色 |
| 菜品管理保存 | 通过 | 价格修改 600ms 防抖，保存后从服务器重载完整数据 |
| 菜品同步 | 通过 | 保存后批量订单表、单人选择、编辑弹窗同步刷新 |
| 编辑弹窗 | 通过 | 打开时强制刷新下拉框 |
| 移动端布局 | 通过 | 订单卡片 grid 列自适应，金额跨列展开 |
| JS 语法 | 通过 | node --check 全部通过 |
