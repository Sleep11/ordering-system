# 下个对话交接

> 当前版本：v2.5.1  
> 当前分支：main / bugfix 已同步  
> 线上状态：https://bawei.rth1.xyz 已部署 v2.5.1

## 已完成

- 订单金额字段：应收、减免、实收、退款。
- 退款按钮和“已退款”状态。
- 多餐食订餐、数量加减、订餐列表。
- 餐品下拉框只显示名称。
- Toast 唯一提示码 `MSG-XXXX` 和复制按钮。
- 菜品保存接口兼容多种 Retinbox 请求体。
- 页面增删改后立即刷新。
- 侧边栏 220px、主内容区最大 1120px。
- 今日统计单行横向滚动。
- 菜品管理列表内部滚动。
- 周月报按应收/实收统计。
- Git 提交信息必须以版本号开头。

## 待办

### 1. 布局继续优化
- 侧边栏继续往左靠、主内容区整体加宽。
- 建议参数：`--sidebar-width: 200px`，`--content-max: 1280px`，内容区 padding 加大。
- 需要验证 1440、390、320 三档视口无横向溢出。

### 2. 订单列表价格与行高
- 当前订单列表没有单独展示“价格/原价”。
- 需要恢复价格字段，同时保留应收、减免、实收、退款。
- 每行高度压缩，方便密集选择。

### 3. 菜品管理与订餐下拉框同步
- 菜品管理增删改后，订餐列表下拉框要立即同步。
- 需要检查 `populateDishDropdowns()` 和 `populateBatchOrderTable()` 的调用链。

### 4. 菜品保存错误
- 用户仍反馈菜品管理保存失败。
- 已定位过 `MSG-VVG2` = “菜单数据格式错误”。
- 下个对话应从后端请求体复现开始，用模拟 `req.body` 验证 `normalizeMenuPayload()`。

## 验证命令

```bash
node --check app.js
node --check api.node.js
node --check auth.node.js
node --check kv-adapter.node.js
```

## 下个对话开始问题

> 继续修复 v2.5.1 的遗留问题：先把侧边栏继续左移并加宽主内容区，再给订单列表恢复原价展示并压缩行高，然后修复菜品管理和订餐下拉框同步，最后用后端请求体复现并解决菜品保存 `MSG-VVG2` 错误，完成后更新文档、提交、推送到 main 并部署。
