# 下个对话交接

> 当前版本：v2.5.1.3
> 当前分支：main / bugfix 已同步  
> 线上状态：https://bawei.rth1.xyz 已部署 v2.5.1.3

## 已完成

- 订单金额字段：应收、减免、实收、退款。
- 退款按钮和“已退款”状态。
- 多餐食订餐、数量加减、订餐列表。
- 餐品下拉框只显示名称。
- Toast 唯一提示码 `MSG-XXXX` 和复制按钮。
- 菜品保存接口兼容多种 Retinbox 请求体。
- 页面增删改后立即刷新。
- 侧边栏 220px、主内容区最大 1120px。
- 今日统计单行横向滚动。
- 菜品管理列表内部滚动。
- 周月报按应收/实收统计。
- Git 提交信息必须以版本号开头。
- 版本号改为四位 `x.y.z.n`，每次变更递增最后一位。
- 订单列表恢复原价展示，行高压缩。
- 菜品管理增删后同步订餐下拉框。
- 保存菜品增加 `menuJson` 兜底，解决 `MSG-VVG2`。
- 侧边栏缩窄到 200px，主内容区加宽到 1280px。

## 待办

- 当前无阻塞性待办。
- 下个对话先对线上 v2.5.1.3 做一轮真实管理员账号验收，重点检查保存菜品、订餐列表多餐食、订单列表价格展示。

## 版本更新规则

- 每次提交都必须更新版本号。
- 小改动递增最后一位，例如 `2.5.1.2` -> `2.5.1.3`。
- 大更新可以跳号，例如功能升级可跳到 `2.6.0.0` 或 `3.0.0.0`，由维护者决定。

## 下个对话规则

用户说“去下个对话 / 下一个对话 / 去一下个对话”时：

1. 自动更新 README、QA、HANDOFF。
2. 递增版本号最后一位并同步 `app.js`、`index.html`、README、QA。
3. 提交并推送 `main`，等待 GitHub Actions 部署成功。
4. 同步并推送 `bugfix`。
5. 给出下个对话的起始问题。

## 验证命令

```bash
node --check app.js
node --check api.node.js
node --check auth.node.js
node --check kv-adapter.node.js
```

## 下个对话开始问题

> 请验收线上 v2.5.1.3：确认侧边栏和主区布局、订单列表原价与行高、菜品管理与订餐下拉框同步、保存菜品不再出现 `MSG-VVG2`；如仍有问题，先提供新的提示码或截图，再继续修复。
